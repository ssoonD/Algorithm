#include <iostream>
#include <stack>
#include <vector>
#include <string>
#include <algorithm>
#include <sstream>
using namespace std;

bool chk[10];

int solution(string s) {
	int answer = stoi(s) - 100;
	int len = s.size();
	int tmp = 0;
	for (int i = 0; i < len; i++) {

	}
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0); cout.tie(0);
	string s; cin >> s;
	int m; cin >> m;
	while (m--) {
		int num; cin >> num;
		chk[num] = false;
	}
}