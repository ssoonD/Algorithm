#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
	int n, find_num;
	cin >> n >> find_num;
	vector<int> v;
	for (int i = 0; i < n; i++) {
		v.push_back()
	}
}